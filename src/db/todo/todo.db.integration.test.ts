/**
 * @group integration
 */

describe('PersonDb integration', () => {
  // const user = fakeUser();

  // beforeAll(async () => {
  //   initIntegrationTests();
  //   await connectToMongoDb();
  // });

  // beforeEach(async () => {
  //   await getPersonModel().deleteMany();
  // });

  // afterEach(() => {
  //   jest.resetAllMocks();
  // });

  // afterAll(async () => {
  //   await disconnectFromMongoDb();
  // });

  test('should create a person', async () => {
    // Given
    // When
    // Then
    expect(true).toBeTruthy();
  });
});
